spring.application.name=Config-Server

server.port=8094


#when using default port for service-registry 8761 then no need to give eureka config. eg:below
#eureka.client.register-with-eureka=true
#eureka.client.fetch-registry=true
#eureka.client.service-url.defaultZone=http://localhost:8761/eureka
#eureka.instance.prefer-ip-address=true

spring.cloud.config.server.git.uri=https://github.com/dinesh-b-09/RateMyStay-config

#it cannot import own config from itself
#spring.config.import= optional:configserver:http://localhost:8094

#spring.profiles.active=prod
#spring.profiles.active=dev
